func consume(values [3]int) {
}

func main() {
    mut values := [3]int{1, 2, 3}
    index := 1
    values[index] = 5
    mut total := 0
    for i, value := range values {
        total = total + i + value
    }
    total = total + values[index] + len(values)
    print(total)
    consume(values)
}
