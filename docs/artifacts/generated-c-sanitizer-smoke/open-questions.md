# Open Questions: generated-c-sanitizer-smoke

- None for this slice.

Future work can decide whether `mlg build` should expose a dedicated sanitizer
mode or whether sanitizer coverage should stay as an agent-maintained smoke
inside `scripts/check.sh`.
