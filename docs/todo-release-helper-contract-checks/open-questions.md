# Open Questions: release-helper-contract-checks

No open language-design questions. Remote publish still requires explicit user
approval because the finalizer moves the selected bookmark and pushes it.
