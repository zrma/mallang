import "std/fs"
import "std/os"

func main() {
    match os.args() {
        case Ok(args) {
            match fs.readText(con args[1]) {
                case Ok(_) {
                    print("ok")
                }
                case Err(error) {
                    print(error.kind)
                }
            }
        }
        case Err(error) {
            print(error.kind)
        }
    }
}
